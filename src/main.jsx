import React from "react";
import ReactDOM from "react-dom/client";
import TranspacWebsite from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TranspacWebsite />
  </React.StrictMode>
);
